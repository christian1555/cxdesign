/**
 * Font Normalizer Module — DISABLED
 *
 * Stubs only. No normalization is applied.
 */

(function() {
    'use strict';
    window.TrueTab = window.TrueTab || {};

    async function applyGlobalFontNormalization() {}
    function clearFontCache() {}
    function normalizeWidgetElements() {}

    window.TrueTab.applyGlobalFontNormalization = applyGlobalFontNormalization;
    window.TrueTab.clearFontCache = clearFontCache;
    window.TrueTab.normalizeWidgetElements = normalizeWidgetElements;
})();
